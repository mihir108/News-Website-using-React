{
  "name": "news-app",
  "version": "1.0.0",
  "description": "My news App",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd client && npm install && npm run build"
  },
  "author": "Mihir Kumar Srivastava",
  "license": "ISC",
  "engines": {
    "node": "14.16.1"
  },
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "newsapi": "^2.4.1"
  }
}
